"use strict";

var HMRC = {} || HMRC;

HMRC.PageSetup = {
  init: function() {
    console.log("------- page initial actions goes here");
  }
};

//initializze the actions on DOM loads
HMRC.PageSetup.init();
